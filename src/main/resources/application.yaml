spring:
  application:
    name: site_dahalani
  datasource:
    url: jdbc:postgresql://localhost:5432/dahalanidb
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081
